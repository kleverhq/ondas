# Optional devcontainer profile, relative to the repository root.
# ONDAS_DEV_CONFIG=.devcontainer.local/devcontainer.json

# Optional absolute host path containing fixture provider directories.
# ONDAS_FIXTURES=/absolute/path/to/fixtures
